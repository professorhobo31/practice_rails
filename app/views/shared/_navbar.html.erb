<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <%= link_to "Home", root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <%= link_to "Inicio", root_path, class: "nav-link" %>
                <%= link_to "Contáctenos", about_path, class: "nav-link" %> <%# This line works like so: the first string defines the text inside the link, the about_path references where to go %>
            </div>
            <div class="navbar-nav ms-auto me-3 nav-link">
                <% if Current.user %>
                <div class="navbar-collapse collapse container text-wrap text-center mt-1 gap-1" style="width: 10rem;" id="navbarNavAltMarkup">
                    <%= Current.user.email%>
                </div>
                <%= link_to "Editar", passwords_path, class: "nav-link mt-1 me-2"%>
                <%= button_to "Desloguearse", logout_path, method: :delete, class: "btn btn-outline-danger mt-1" %>
                <% else %>
                <%= link_to "Ingresar", sign_in_path, class: "nav-link" %>
                <%= link_to "Subscribirse", sign_up_path, class: "nav-link" %>
                <% end %>
            </div>
        </div>
    </div>
</nav>