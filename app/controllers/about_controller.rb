class AboutController < ApplicationController # we need to inherit from this class to get all the Rails functionality
  def index # We need to define an action w/the same name that we wrote in routes.rb, however it need not do anything as the default inherited action is enough for us at this point
  end
end
